#include <stdio.h>

int main(int argc, char *argv[]) {
	float numero;
	printf("Escribir un número: ");
	scanf("%f",&numero);
	if(numero<0){
		numero = numero * -1;
	}
	printf("Valor absoluto = %.2f",numero);
	return 0;
